fastapi==0.111.0
uvicorn[standard]==0.30.1
telethon==1.36.0
python-dotenv==1.0.1
pydantic==2.11.7
python-multipart==0.0.9
Pillow==11.3.0
cryptg==0.6.0  # Native AES — big upload/download speedup (prebuilt wheels available)
# pillow-heif==0.21.0  # Optional: HEIC/HEIF EXIF support for iPhone photos
# mutagen==1.47.0      # Optional: video metadata date extraction
