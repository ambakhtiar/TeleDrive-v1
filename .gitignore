# Security & Credentials 
.env
backup_session.session
backup_session.session-journal

# Database & Logs (local data)
uploads.db
uploader.log

# Dynamic State Files (legacy two-process IPC — no longer used)
progress.json
queue.json
state.json
command.json
config.json
send_report.trigger

# Browser-upload staging area
uploads_staging/

# Python Bytecode (unnecessary cache file)
__pycache__/

